"""
Auto-generated contract wrapper for Token
Generated by contract-sdk-gen
"""

from typing import Optional
from web3 import Web3
from web3.contract import Contract
from .abi import ABI

class Token:
    """Token contract"""
    
    def __init__(self, web3: Web3, address: str):
        self.web3 = web3
        self.address = Web3.to_checksum_address(address)
        self.contract = web3.eth.contract(address=self.address, abi=ABI)

    def d_o_m_a_i_n__s_e_p_a_r_a_t_o_r(self):
        """DOMAIN_SEPARATOR"""
        return self.contract.functions.DOMAIN_SEPARATOR().call()

    def allowance(self, owner: str, spender: str):
        """allowance"""
        return self.contract.functions.allowance(owner, spender).call()

    def approve(self, spender: str, value: int):
        """approve"""
        tx = self.contract.functions.approve(spender, value).transact()
        return tx

    def approved(self, arg0: str):
        """approved"""
        return self.contract.functions.approved(arg0).call()

    def balance_of(self, account: str):
        """balanceOf"""
        return self.contract.functions.balanceOf(account).call()

    def burn(self, from_param: str, amount: int):
        """burn"""
        tx = self.contract.functions.burn(from_param, amount).transact()
        return tx

    def decimals(self):
        """decimals"""
        return self.contract.functions.decimals().call()

    def eip712_domain(self):
        """eip712Domain"""
        return self.contract.functions.eip712Domain().call()

    def initialize(self, owner: str, network: str):
        """initialize"""
        tx = self.contract.functions.initialize(owner, network).transact()
        return tx

    def mint(self, to: str, amount: int):
        """mint"""
        tx = self.contract.functions.mint(to, amount).transact()
        return tx

    def name(self):
        """name"""
        return self.contract.functions.name().call()

    def network(self):
        """network"""
        return self.contract.functions.network().call()

    def nonces(self, owner: str):
        """nonces"""
        return self.contract.functions.nonces(owner).call()

    def owner(self):
        """owner"""
        return self.contract.functions.owner().call()

    def permit(self, owner: str, spender: str, value: int, deadline: int, v: int, r: bytes, s: bytes):
        """permit"""
        tx = self.contract.functions.permit(owner, spender, value, deadline, v, r, s).transact()
        return tx

    def renounce_ownership(self):
        """renounceOwnership"""
        tx = self.contract.functions.renounceOwnership().transact()
        return tx

    def symbol(self):
        """symbol"""
        return self.contract.functions.symbol().call()

    def total_supply(self):
        """totalSupply"""
        return self.contract.functions.totalSupply().call()

    def transfer(self, to: str, value: int):
        """transfer"""
        tx = self.contract.functions.transfer(to, value).transact()
        return tx

    def transfer_from(self, from_param: str, to: str, value: int):
        """transferFrom"""
        tx = self.contract.functions.transferFrom(from_param, to, value).transact()
        return tx

    def transfer_ownership(self, new_owner: str):
        """transferOwnership"""
        tx = self.contract.functions.transferOwnership(new_owner).transact()
        return tx

    def update_whitelist(self, address: str, approved: bool):
        """updateWhitelist"""
        tx = self.contract.functions.updateWhitelist(address, approved).transact()
        return tx
